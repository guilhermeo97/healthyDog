export type School = {
  id?: number;
  name: string;
  cnpj: string;
  email: string;
  phone: string;
  address: string;
  state?: boolean | number;
  created?: Date;
};

// id,
// name,
// cnpj,
// email,
// phone,
// address,
// state,
// created,
